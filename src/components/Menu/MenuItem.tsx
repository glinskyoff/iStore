import React from "react";
import styles from "./Menu.module.scss";

import { Phone } from "./images/Phone";

export const MenuItem: React.FC = () => {
  return (
    <h1>123</h1>
    // <button className={styles.menuItem} onClick={() => handleChangeModel(id, model)}
    // style={{
    //   background: id ===  ? "rgba(100, 102, 105, 0.25)" : "transparent",
    // }}>
    //   <Phone />
    //   <p className={styles.menuItemTitle}>{model}</p>
    // </button>
  );
};
